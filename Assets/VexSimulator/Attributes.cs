namespace VexSimulator
{
    /**
     * This attribute doesn't actually do anything, just tell's the programmer that this is a threaded method
     */
    public class ThreadedMethod : System.Attribute
    {
    }
}